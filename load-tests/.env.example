# Copy to load-tests/.env and source before k6 (do not commit .env)
# export $(grep -v '^#' load-tests/.env | xargs)   # optional

BASE_URL=http://localhost:4173
SUPABASE_URL=https://sgkpvwrunlrhegohorcz.supabase.co
SUPABASE_ANON_KEY=sb_publishable_ykoReR1rvoKhuH4gQ20fZA_BaI3VQfV
# Optional: real JWT if RLS requires auth (get from browser session)
# SUPABASE_JWT=
# Optional: existing event UUID for detail-style batch requests
# EVENT_ID=
